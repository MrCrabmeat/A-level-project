#create Binary converter for an integer input

UserInput = ""
UserNum = 0
ValidInput = False

#procedures:
def switch(UserNum, UserInput)
	UserInput = input("what is your number? ")
	ValidInput = False
	while ValidInput = False
		#check if this input is an int
		if UserInput.isdigit():
			UserNum = int(UserInput)
			if UserNum < 0 or UserNum > 255:
				print("please input a integer between 0 and 255")
			else:
				VaildInput = True
		else: 
				print("please input an integer number")
